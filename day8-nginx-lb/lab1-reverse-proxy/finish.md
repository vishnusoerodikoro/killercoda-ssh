# Lab 1 Selesai!

## Yang sudah kamu pelajari:
- NGINX sebagai reverse proxy
- `proxy_pass` directive
- Forward headers (X-Real-IP, X-Forwarded-For)
- Membaca access log NGINX
- Perbedaan akses langsung vs lewat proxy

## Key Takeaway
> Reverse proxy menyembunyikan backend dari client. Client hanya tahu NGINX di port 80, tidak tahu ada Python server di belakangnya.

Lanjut ke **Lab 2 — Round Robin Load Balancing!**
